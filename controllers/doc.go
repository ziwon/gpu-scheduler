// Package controllers holds future controller-runtime Reconcilers.
// The MVP does not require controllers, but we keep the module in place
// to satisfy kubebuilder layout expectations.
package controllers
